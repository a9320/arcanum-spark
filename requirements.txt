# CodeRisk Cloud × Arcanum — Python 依赖（Python 3.10+）
# 说明：PITAX 检测引擎（app/pitax/）零第三方依赖，纯标准库即可运行。
# 版本策略：一律范围约束——创空间构建环境（gradio 6.17.3 要求 fastapi>=0.115.2 /
# httpx>=0.28.1 / python-multipart>=0.0.18），精确钉版会 ResolutionImpossible。

# ── API 服务层（legacy 平台）──
fastapi>=0.115.2,<1.0
uvicorn[standard]>=0.32
pydantic>=2.9,<3
python-multipart>=0.0.18

# ── 任务队列 ──
redis>=5.2
celery>=5.4

# ── 引擎层（engine/，传统漏洞检测：静态分析/污点/依赖/LLM 语义）──
rich>=13.0        # 引擎控制台输出（硬依赖）
httpx>=0.28       # Nutrient DWS PDF + LLM client（llama.cpp OpenAI 兼容接口）

# ── 测试 ──
pytest>=8.3

# ── 魔搭创空间部署入口（gradio 类型 app.py + streamlit 备用入口）──
gradio>=5.0

# ── 6-agent 流水线（codeark/，verify.sh 一键验证所需）──
strands-agents>=1.55
openai>=3.0
